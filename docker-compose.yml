name: booknook

services:
  web:
    image: rubyfrost101/booknook:prod
    pull_policy: always
    container_name: booknook
    restart: unless-stopped
    user: "${PUID:-1000}:${PGID:-1000}"
    environment:
      STORAGE_ROOT: /app/storage
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "${WEB_PORT:-7209}:3000"
    volumes:
      - ${STORAGE_PATH:-./data/storage}:/app/storage
      - ${MONITOR_HOST_PATH:-./monitor}:/monitor
      # Add more host libraries as needed, for example:
      # - /path/to/another-library:/libraries/another
    command: ["./scripts/start-unified-app.sh"]
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 30s
  manga-downloader:
    image: yxb900211/komga-manga-downloader:1.1.9
    container_name: komga-manga-downloader
    ports:
      - "45458:8080"
    environment:
      LICENSE_CODE: '$$Yxb=900211'
      AUTH_USERNAME: admin
      AUTH_PASSWORD: 123456
      JWT_SECRET: 4C99392CE4CBA06B547495462F4387CC
      DATA_DIR: /app/data
      KOMGA_LIBRARY_PATH: /data/komga
      KXO_BASE_URL: https://kxo.moe
      KXO_MIRRORS: kxo.moe,kxx.moe,kzz.moe,koz.moe
      COOKIE_REFRESH_MINUTES: 30
      SYNC_INTERVAL_MINUTES: 60
      MAX_DOWNLOAD_WORKERS: 2
      ORGANIZE_ROOT_PATHS: /data/staging
    volumes:
      - ./mange:/app/data
      - ./monitor:/data/komga
      - ./漫画:/data/staging
    restart: unless-stopped
