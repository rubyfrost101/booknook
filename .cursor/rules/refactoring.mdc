---
description: 按业务能力渐进重构：表征测试、提取规则、端口、用例、切换入口、删除双写。
alwaysApply: true
---

# 重构标准

一次只迁移一个可命名业务能力。不要一次性全仓搬迁，也不要仅为缩短文件而改变事务/状态码/字段/Worker 语义。

## 步骤

1. 盘点入口、调用者、角色、契约、状态变化、副作用
2. 为当前公开行为补表征测试
3. 提取纯规则与显式类型（先不动 I/O）
4. 触及的 raw SQL 迁到 ORM
5. 引入能力内端口与 adapter
6. 编排与事务收到 application 用例
7. route / Worker / UI 变为薄适配
8. 调用方切换后删除旧实现；禁止长期双写
9. 跑聚焦与适用全量门禁
10. 跨能力或兼容策略变化写 ADR

## PR 约束

- 只覆盖一个业务能力或一个清晰基础设施边界
- 说明不变量与未改变的公开行为
- 不混入无关格式化、依赖升级、产品改版
- 可独立回滚；临时兼容层有 owner、测试与退出条件

## 例外

无法立即合规时，在代码旁或 ADR 记录：违反规则、阻塞原因、风险与保护测试、责任模块、清理条件。禁止“后续优化”式退出。
