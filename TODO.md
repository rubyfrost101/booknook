# BookNook Starship MVP Todolist

## 状态说明

- `[x] 已完成`
- `[-] 进行中`
- `[ ] 待开始`

## MVP 任务总览

- `[x]` Task 01：创建 Node.js 全栈 Monorepo 脚手架
- `[x]` Task 02：迁移高保真原型为 Next.js 页面
- `[x]` Task 03：设计 MVP 数据模型
- `[x]` Task 04：实现基础认证
- `[x]` Task 05：实现书库路径配置
- `[x]` Task 06：实现基础书库 API
- `[x]` Task 07：实现 NAS 目录扫描 Worker
- `[x]` Task 08：实现扫描任务 API 和页面
- `[x]` Task 09：实现读物列表和详情页接入真实 API
- `[x]` Task 10：实现文件受控访问 API
- `[ ]` Task 11：TXT 阅读器（历史目标，当前版本暂不支持 TXT 导入）
- `[x]` Task 12：实现 PDF 阅读器
- `[x]` Task 13：实现图片 / 漫画阅读器
- `[x]` Task 14：实现阅读进度保存与恢复
- `[x]` Task 15：实现移动端响应式基础页面
- `[x]` Task 16：实现 Docker Compose 部署
- `[x]` Task 17：补充 README、测试数据和验收脚本

## MVP 验收结果

- `[x]` Task 01 验收通过：Monorepo、Turborepo、健康检查和构建命令可用。
- `[x]` Task 02 验收通过：主要 Next.js 页面和导航可构建渲染。
- `[x]` Task 03 验收通过：SQLite schema 覆盖 MVP 核心实体并可自动初始化。
- `[x]` Task 04 验收通过：登录、会话、退出和受保护 API 已实现。
- `[x]` Task 05 验收通过：书库路径配置 API 与设置页已接入真实持久化。
- `[x]` Task 06 验收通过：读物列表、详情、更新和隐藏 API 已实现。
- `[x]` Task 07 验收通过：扫描 Worker 可识别 EPUB、PDF、CBZ/ZIP 漫画压缩包并 upsert 数据库。
- `[x]` Task 08 验收通过：扫描任务 API 与页面可创建任务、查看进度、日志和历史。
- `[x]` Task 09 验收通过：书库列表和详情页已接入真实 API。
- `[x]` Task 10 验收通过：受控文件 API 支持认证、根路径约束、Range 和流式返回。
- `[ ]` Task 11 验收未纳入当前版本：TXT 导入与阅读器暂不支持。
- `[x]` Task 12 验收通过：PDF 阅读器通过受控 API 打开，支持页码、缩放控制和进度恢复。
- `[x]` Task 13 验收通过：图片 / 漫画阅读器通过受控 API 按页阅读并保存进度。
- `[x]` Task 14 验收通过：统一阅读进度 API 和节流保存已接入三类阅读器。
- `[x]` Task 15 验收通过：核心页面和阅读器已完成移动端基础响应式适配。
- `[x]` Task 16 验收通过：Docker Compose 覆盖统一 Web/API/Worker、SQLite 和书库挂载。
- `[x]` Task 17 验收通过：README、测试数据、自动验收脚本和手工验收清单已补齐。

自动验收命令：`pnpm acceptance` 已通过。

## 详细任务清单

### Task 01：创建 Node.js 全栈 Monorepo 脚手架

状态：`[x] 已完成`

目标：建立 MVP 的基础工程结构，让 Web、共享包、数据库包、后台 Worker 和部署配置可以在同一个仓库内协同开发。

实现内容：
- 使用 pnpm workspace 管理 `apps/*`、`packages/*`、`workers/*`。
- 使用 Turborepo 串联 `dev`、`build`、`lint`、`typecheck` 等基础命令。
- 保留 `apps/web` 作为 Next.js 前端入口，API 由 Python 后端提供。
- 保留 `packages/shared`、`packages/ui`、`packages/reader-core` 作为 MVP 共享包边界。
- 使用 Python watchdog worker 作为扫描任务 Worker 入口。

验收标准：
- 根目录可以通过 `pnpm install` 安装依赖。
- 根目录可以通过 `pnpm dev` 启动开发服务。
- 根目录可以通过 `pnpm build` 执行构建任务。
- Python `/api/health` 可返回健康检查结果。

### Task 02：迁移高保真原型为 Next.js 页面

状态：`[x] 已完成`

目标：把 MVP 高保真原型迁移为可运行的 Next.js 页面，先完成主要信息架构和交互外观。

实现内容：
- 使用 Next.js App Router 建立首页、书库、书架、整理、扫描任务、设置、登录、阅读器、移动端预览等页面。
- 将原型拆分为页面级 feature 组件和可复用 UI 组件。
- 使用 Tailwind CSS 与主题样式承接视觉规范。
- 当前阶段可使用 mock 数据，但页面结构需要为后续接真实 API 预留清晰边界。

验收标准：
- 主要页面路由可直接访问且无运行时错误。
- 页面组件不依赖外部原型工具即可渲染。
- 导航入口能覆盖 MVP 主要页面。
- mock 数据集中维护，后续可以替换为 API 数据。

### Task 03：设计 MVP 数据模型

状态：`[x] 已完成`

目标：设计支撑书库、文件、扫描任务、阅读进度和基础认证的 MVP 数据模型。

实现内容：
- 扩展当前极简 `LibraryItem` 占位模型。
- 增加用户、会话或认证凭据相关模型。
- 增加书库路径配置模型，记录 NAS 根路径、启用状态和扫描策略。
- 增加读物、文件、章节或资源条目模型，支持 EPUB、PDF、图片 / 漫画等格式。
- 增加扫描任务和扫描日志模型，记录状态、进度、错误和耗时。
- 增加阅读进度模型，记录用户、读物、当前位置、百分比和更新时间。

验收标准：
- SQLite schema 能覆盖 MVP 的核心实体与关系。
- 应用首次启动能自动创建完整 schema。
- 模型字段足够支持 Task 04 到 Task 14 的 API 与页面需求。
- 不引入超出 MVP 的复杂权限、多租户或推荐系统模型。

### Task 04：实现基础认证

状态：`[x] 已完成`

目标：提供单用户或轻量多用户的基础登录能力，保护管理页面、阅读页面和文件访问 API。

实现内容：
- 实现登录页表单和登录 API。
- 实现密码校验、会话创建、会话续期和退出登录。
- 增加受保护路由逻辑，未登录用户跳转到登录页。
- 增加 API 鉴权中间件或统一 helper，保护书库、扫描、阅读进度和文件访问接口。
- 为本地部署提供初始管理员账号配置方式。

验收标准：
- 未登录访问受保护页面会被拦截。
- 登录成功后可以访问书库和阅读页面。
- 退出登录后会话失效。
- 未认证请求无法访问受控文件 API。

### Task 05：实现书库路径配置

状态：`[x] 已完成`

目标：允许用户配置 NAS 上的读物根目录，并为后续扫描任务提供稳定输入。

实现内容：
- 在设置页增加书库路径配置表单。
- 实现书库路径的新增、编辑、启用 / 禁用和删除。
- 在服务端校验路径格式、路径是否存在、是否可读。
- 将书库路径配置持久化到数据库。
- 提供 API 给扫描任务选择目标书库路径。

验收标准：
- 用户可以在页面上保存至少一个书库根路径。
- 不存在或不可读路径会给出明确错误。
- 禁用的路径不会进入扫描任务。
- 已保存路径刷新页面后仍然存在。

### Task 06：实现基础书库 API

状态：`[x] 已完成`

目标：提供读物数据的基础增删改查能力，作为列表、详情、阅读器和扫描入库的统一数据来源。

实现内容：
- 实现读物列表 API，支持分页、搜索、格式筛选和基础排序。
- 实现读物详情 API，返回元数据、文件信息、章节 / 页信息和阅读进度摘要。
- 实现读物元数据更新 API，支持标题、作者、封面、标签或描述等 MVP 字段。
- 实现读物删除或隐藏 API，避免误删 NAS 源文件。
- 定义统一响应格式和错误处理方式。

验收标准：
- 前端可以通过 API 获取读物列表和详情。
- 搜索、分页和格式筛选结果稳定。
- 更新元数据后列表与详情能看到最新值。
- 删除或隐藏操作不会直接破坏源文件。

### Task 07：实现 NAS 目录扫描 Worker

状态：`[x] 已完成`

目标：实现 Python 后台扫描 Worker，从配置的 NAS 目录中发现读物文件并写入数据库。

实现内容：
- 基于 Python watchdog 监听监控目录。
- 在 Python Worker 中实现目录遍历、文件类型识别和基础元数据提取。
- 支持 EPUB、PDF、CBZ/ZIP 漫画压缩包的 MVP 文件识别。
- 将扫描结果 upsert 到数据库，避免重复入库。
- 记录扫描进度、发现数量、跳过数量、错误日志和完成状态。

验收标准：
- Worker 可以消费扫描任务并输出真实扫描结果。
- 重复扫描同一目录不会产生重复读物。
- 文件不可读或格式不支持时记录错误但不中断整个任务。
- 扫描完成后书库 API 能查到新增读物。

### Task 08：实现扫描任务 API 和页面

状态：`[x] 已完成`

目标：提供创建、查看和追踪扫描任务的 API 与页面，连接 Web 和后台 Worker。

实现内容：
- 实现创建扫描任务 API，接收书库路径配置 ID。
- 实现扫描任务列表和详情 API，返回状态、进度、日志和错误。
- 页面接入真实 API，展示任务队列、运行中任务、完成任务和失败任务。
- 支持手动触发扫描和刷新任务状态。
- 对重复触发、路径不存在、Worker 不可用等情况给出明确反馈。

验收标准：
- 用户可以从页面手动创建扫描任务。
- 任务创建后 Worker 能消费并更新状态。
- 页面能看到任务进度、完成结果和失败原因。
- 刷新页面后任务历史仍然存在。

### Task 09：实现读物列表和详情页接入真实 API

状态：`[x] 已完成`

目标：用真实数据库和 API 替换当前 mock 数据，让书库浏览进入可用状态。

实现内容：
- 列表页调用读物列表 API，处理加载、空状态、错误状态和分页状态。
- 详情页调用读物详情 API，展示真实元数据、文件信息和阅读入口。
- 保留现有视觉结构，替换数据来源和交互逻辑。
- 在读物缺失或无权限时展示明确错误。

验收标准：
- 扫描入库后的读物会出现在列表页。
- 点击读物可进入真实详情页。
- 列表和详情不再依赖 mock 数据作为主数据源。
- API 失败时页面不会崩溃。

### Task 10：实现文件受控访问 API

状态：`[x] 已完成`

目标：所有读物源文件必须通过受控 API 访问，避免直接暴露 NAS 真实路径。

实现内容：
- 实现文件访问 API，根据读物或文件 ID 定位源文件。
- 做认证校验和读物权限校验。
- 做路径归一化和根目录约束，防止路径穿越。
- 支持 `Range` 请求和流式返回，满足 PDF、图片和大文本阅读。
- 设置合适的 Content-Type、Content-Disposition、缓存和错误响应。

验收标准：
- 未登录用户无法读取文件。
- 请求无法越过已配置书库根目录。
- 大文件可以分段或流式读取。
- EPUB、PDF 和漫画阅读器都通过该 API 获取内容。

### Task 11：TXT 阅读器（历史目标，当前暂不支持）

状态：`[ ] 当前版本暂不支持`

目标：如果后续重新纳入 TXT，再提供可用的 TXT 阅读体验，支持基础排版、位置恢复和进度保存。

实现内容：
- 暂不恢复 TXT 导入；当前支持格式统一为 EPUB、PDF、CBZ、ZIP。
- 后续如恢复 TXT，需要同步更新导入白名单、阅读器类型、文档和验收。

验收标准：
- 当前版本上传 TXT 应给出不支持格式提示。
- 所有页面和文档不再声称 TXT 已支持。

### Task 12：实现 PDF 阅读器

状态：`[x] 已完成`

目标：提供浏览器内 PDF 阅读能力，支持翻页、缩放和进度保存。

实现内容：
- 通过受控文件 API 加载 PDF 文件。
- 使用浏览器原生 PDF 渲染，支持页码、上一页、下一页和缩放。
- 记录当前页码和阅读百分比。
- 进入阅读器时恢复上次页码。
- 处理 PDF 加载失败、密码保护或不支持文件的提示。

验收标准：
- PDF 文件可以在阅读器中打开。
- 用户可以翻页和缩放。
- 关闭后再次打开能恢复到上次页码。
- PDF 文件不会暴露 NAS 真实路径。

### Task 13：实现图片 / 漫画阅读器

状态：`[x] 已完成`

目标：支持图片集合或漫画目录的分页阅读体验。

实现内容：
- 通过读物详情 API 获取图片页列表。
- 每一页图片通过受控文件 API 加载。
- 支持单页、双页和基础缩放；当前暂不支持滚动阅读模式。
- 支持页码跳转、上一页、下一页和阅读方向的 MVP 配置。
- 保存当前页码和阅读百分比。

验收标准：
- 图片 / 漫画读物可以按页顺序阅读。
- 图片加载失败不会导致整个阅读器崩溃。
- 重新进入后能恢复到上次页码。
- 所有图片访问都经过受控文件 API。

### Task 14：实现阅读进度保存与恢复

状态：`[x] 已完成`

目标：为 EPUB、PDF、图片 / 漫画阅读器提供统一的阅读进度保存与恢复能力。

实现内容：
- 在 SQLite schema 中增加阅读进度模型。
- 实现读取进度 API，按用户和读物返回最新进度。
- 实现保存进度 API，支持当前位置、页码、百分比和阅读器类型扩展数据。
- 在阅读器中做节流保存，避免高频请求。
- 进入读物详情和阅读器时展示或恢复最近进度。
- MVP 阶段采用最后写入优先的多设备基础策略。

验收标准：
- 三类阅读器都能保存并恢复进度。
- 同一用户不同读物的进度互不影响。
- 刷新页面或重新登录后进度仍然存在。
- 高频翻页或跳转不会产生过量 API 请求。

### Task 15：实现移动端响应式基础页面

状态：`[x] 已完成`

目标：保证 MVP 的核心页面在手机浏览器上可用，覆盖浏览、详情、阅读和设置等基础路径。

实现内容：
- 优化全局布局在小屏幕下的导航和内容密度。
- 为书库列表、详情页、扫描任务页、设置页和阅读器适配移动端。
- 浏览器与安装后的 PWA 共用同一套响应式页面，不维护独立移动预览或适配层。
- 检查按钮、表单、表格和阅读区域在窄屏下不溢出。

验收标准：
- 主要页面在 375px 宽度下没有明显横向溢出。
- 移动端可以完成登录、浏览读物、进入阅读器和查看扫描任务。
- 阅读器控制栏不会遮挡主要内容。
- 桌面端布局不因移动端适配而退化。

### Task 16：实现 Docker Compose 部署

状态：`[x] 已完成`

目标：提供 MVP 自托管部署方式，一条命令启动统一 Web、API 和 Worker。

实现内容：
- 完善 `docker-compose.yml` 中的统一应用服务。
- 为 Web 和 Worker 提供 Dockerfile 构建配置。
- 配置 SQLite 持久化目录、端口、书库 volume 和环境变量。
- 增加数据库迁移或初始化流程。
- 支持挂载 NAS 书库目录到容器内固定路径。

验收标准：
- `docker compose up --build` 可以启动完整服务。
- Web/API 能自动创建并连接 SQLite。
- Worker 能直接消费本地任务队列。
- 容器内可以访问挂载的书库目录。

### Task 17：补充 README、测试数据和验收脚本

状态：`[x] 已完成`

目标：补齐 MVP 使用说明、演示数据和可重复执行的验收流程，降低本地开发和部署验证成本。

实现内容：
- 更新 README，补充本地开发、环境变量、数据库迁移、扫描目录配置和 Docker 部署说明。
- 增加测试数据目录或 seed 脚本，覆盖 EPUB、PDF、图片 / 漫画等 MVP 格式。
- 增加验收脚本或命令，串联安装、类型检查、构建、数据库准备和健康检查。
- 补充 MVP 手工验收清单，覆盖登录、配置路径、扫描、浏览、阅读和进度恢复。

验收标准：
- 新开发者可以按 README 在本地跑起 MVP。
- seed 或测试数据能支持核心页面展示和阅读器验证。
- 验收脚本可以发现明显构建或配置问题。
- 手工验收清单覆盖 Task 04 到 Task 16 的核心路径。
